{
  "name": "@tagmytaxi/matching-service",
  "version": "2.0.0",
  "description": "Ride-driver matching engine for the TagMyTaxi platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@tagmytaxi/shared": "*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.2"
  }
}
